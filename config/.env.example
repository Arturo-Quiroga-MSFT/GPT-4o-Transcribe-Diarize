# Azure OpenAI Configuration Template
# Copy this file to .env and fill in your actual values

# Azure OpenAI Endpoint (required)
# Format: https://YOUR-RESOURCE-NAME.openai.azure.com
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com

# Azure OpenAI API Key (required if not using Entra ID)
# Get this from Azure Portal > Your OpenAI Resource > Keys and Endpoint
AZURE_OPENAI_API_KEY=your-api-key-here

# Azure OpenAI API Version (required)
# Use the latest preview version for gpt-4o-transcribe-diarize
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# Model Deployment Name (optional, defaults to gpt-4o-transcribe-diarize)
# This is the name you gave to your deployment in Azure Portal
MODEL_DEPLOYMENT_NAME=gpt-4o-transcribe-diarize

# Authentication Method (optional)
# Set to "entra_id" to use Microsoft Entra ID, otherwise API key is used
# AUTH_METHOD=api_key

# Notes:
# - Never commit the actual .env file to version control
# - The .env file should be listed in .gitignore
# - For production, consider using Azure Key Vault or Managed Identity
# - For local development with Entra ID, ensure you're logged in with Azure CLI:
#   az login
